<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Update Campaign</title>

<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">

<style>
body{
  background: linear-gradient(135deg, #1d976c, #93f9b9);
  min-height:100vh;
  display:flex;
  align-items:center;
  justify-content:center;
  font-family: "Segoe UI", sans-serif;
}

.wrapper{
  background:#fff;
  border-radius:25px;
  overflow:hidden;
  box-shadow:0 15px 40px rgba(0,0,0,0.25);
  max-width:1000px;
  width:100%;
}

.left-panel{
  background: linear-gradient(to bottom, #185a9d, #43cea2);
  color:white;
  padding:50px;
}

.left-panel h2{
  font-weight:bold;
}

.left-panel ul{
  padding-left:20px;
}

.form-panel{
  padding:40px;
}

.form-control, .form-select{
  border-radius:12px;
}

.btn-success{
  border-radius:12px;
  font-size:18px;
}
</style>
</head>

<body>

<div class="wrapper row g-0">

  <!-- LEFT INFO PANEL -->
  <div class="col-md-5 left-panel d-flex flex-column justify-content-center">
    <h2>Update Campaign</h2>
    <p>Modify your donation campaign details.</p>

    <ul>
      <li>✔ Clothes & Food</li>
      <li>✔ Medicine & Education</li>
      <li>✔ Shelter & Gadgets</li>
      <li>✔ Trusted NGO platform</li>
    </ul>

    <p class="mt-4">
      <strong>“Improving a campaign means improving lives.”</strong>
    </p>
  </div>

  <!-- FORM PANEL -->
  <div class="col-md-7 form-panel">

    <h3 class="mb-4 text-warning">
      <i class="fa fa-pen"></i> Update Campaign
    </h3>

    <form action="/api/campaigns/<%= campaign._id %>?_method=PUT" method="POST">

      <!-- Title -->
      <div class="mb-3">
        <label class="form-label">Campaign Title</label>
        <input 
          type="text" 
          name="title" 
          class="form-control" 
          value="<%= campaign.title %>"
          required>
      </div>

      <!-- Description -->
      <div class="mb-3">
        <label class="form-label">Campaign Description</label>
        <textarea 
          name="description" 
          class="form-control" 
          rows="3"
          required><%= campaign.description %></textarea>
      </div>

      <!-- Target + Deadline -->
      <div class="row">
        <div class="col-md-6 mb-3">
          <label class="form-label">Target Amount (₹)</label>
          <input 
            type="number" 
            name="targetAmount" 
            class="form-control" 
            value="<%= campaign.targetAmount %>"
            required>
        </div>

        <div class="col-md-6 mb-3">
          <label class="form-label">Deadline</label>
          <input 
            type="date" 
            name="deadline" 
            class="form-control" 
            value="<%= campaign.deadline.toISOString().split('T')[0] %>"
            required>
        </div>
      </div>

      <!-- Image -->
      <div class="mb-3">
        <label class="form-label">Campaign Image URL</label>
        <input 
          type="text" 
          name="imageUrl" 
          class="form-control" 
          value="<%= campaign.imageUrl %>">
      </div>

      <!-- Category -->
      <div class="mb-4">
        <label class="form-label">Donation Category</label>
        <select name="category" class="form-select" required>
          <option value="Clothes"   <%= campaign.category === "Clothes" ? "selected" : "" %>>Clothes</option>
          <option value="Money"     <%= campaign.category === "Money" ? "selected" : "" %>>Money</option>
          <option value="Medicine"  <%= campaign.category === "Medicine" ? "selected" : "" %>>Medicine</option>
          <option value="Food"      <%= campaign.category === "Food" ? "selected" : "" %>>Food</option>
          <option value="Education" <%= campaign.category === "Education" ? "selected" : "" %>>Education</option>
          <option value="Footwear"  <%= campaign.category === "Footwear" ? "selected" : "" %>>Footwear</option>
          <option value="Stationery"<%= campaign.category === "Stationery" ? "selected" : "" %>>Stationery</option>
          <option value="Shelter"   <%= campaign.category === "Shelter" ? "selected" : "" %>>Shelter</option>
          <option value="Gadgets"   <%= campaign.category === "Gadgets" ? "selected" : "" %>>Gadgets</option>
          <option value="Other"     <%= campaign.category === "Other" ? "selected" : "" %>>Other</option>
        </select>
      </div>

      <!-- Button -->
      <button class="btn btn-success w-100">
        <i class="fa fa-save"></i> Update Campaign
      </button>
         
<div class="text-center mt-3">
  <a href="/" class="btn btn-outline-success">
    ⬅ Back
  </a>
</div>

    </form>

  </div>
</div>

</body>
</html>
